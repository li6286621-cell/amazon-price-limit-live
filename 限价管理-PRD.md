# 限价管理 PRD

## 需求名称

限价管理

## 需求背景

亚马逊运营需对指定站点下的产品维护最低限价，以控制促销与调价底线。当前缺少统一的限价录入、审核与查询能力，运营与管理人员各自维护易产生口径不一致。需建立独立「限价管理」模块，支持双角色录入、差异化审核流程，并提供可追溯的操作日志。

## 需求方案

1. 新增「限价管理」独立模块：仅拥有**文档查看权限**的角色可访问；左侧导航展示名称：**限价管理**。
2. **统一审核**：所有人录入的数据均进入「待审核」，由管理人员另行审核（通过 / 拒绝）。
3. 列表支持多条件筛选、查询/重置、分页展示；支持「录入限价数据」弹窗；行内「审核」「日志」操作。
4. 审核弹窗布局与录入弹窗一致，顶部增加「审核」下拉（待审核 / 审核通过 / 拒绝）。
5. 刊登最低金额币种随站点变化（如日本站显示 JPY）。

**原型文件**：`亚马逊限价管理.html`

---

## 文档编辑人

待定

---

## 需求详情

**页面放置**  
亚马逊相关菜单下（与现有导航结构对齐；左侧导航展示名称：**限价管理**。）

**现有说明**  
原型顶栏当前为「原型」占位，上线替换为统一系统顶栏；列表标题为 **限价管理**。

**原型链接（当前静态页）**  
本地文件：`file:///C:/Users/Admin/11110/亚马逊限价管理/亚马逊限价管理.html`

---

### 权限与数据录入

#### 0.1 模块访问

| 角色类型 | 访问权限 | 录入 | 审核 |
|---------|---------|------|------|
| 仅文档查看权限 | 可访问列表与详情 | 否 | 否 |
| 运营人员 | 可访问 | 可录入，提交后待审核 | 否 |
| 管理人员 | 可访问 | 可录入，提交后同样待审核 | 可审核 |

**说明**：「仅拥有文档查看权限的角色可访问」指该模块对具备文档查看权限的用户开放；具体增删改、审核能力由角色细分控制（与系统统一 RBAC 对齐）。

#### 0.2 录入与审核规则

| 操作 | 审核状态 | 说明 |
|------|---------|------|
| 任何人录入 | 待审核 | 审核人为空，需管理人员审核 |
| 管理人员审核通过 | 审核通过 | 写入审核人 |
| 管理人员拒绝 | 拒绝 | 写入审核人；建议填写备注 |

**拒绝后**：可再次打开「审核」调整数据并改状态（本期列表「审核」按钮对待审核、拒绝状态可见）。

---

### 限价管理列表页

#### 1.1 筛选查询功能

**产品 ID**  
文本输入框；**精确匹配**；支持多个，以**空格或英文逗号**分隔；默认为空。

**站点**  
下拉选择框；**支持多选**；选项为业务站点（如：日本、美国、英国、德国、法国等）；默认为空（表示全部）。

**审核状态**  
下拉选择框；**支持多选**；枚举：**待审核**、**审核通过**、**拒绝**；默认为空（表示全部）。

**添加人**  
下拉选择框；选项为系统用户；默认为空（全部）。

**审核人**  
下拉选择框；选项为系统用户；默认为空（全部）。

**查询**  
点击查询按钮发起列表查询；从第一页加载（已接服务端分页时）。

**重置**  
清空所有筛选项；多选下拉恢复为空（全部）。

**布局**  
「查询」「重置」按钮紧靠筛选项后；其下方单独一行放置「录入限价数据」「删除」按钮。

#### 1.2 列表展示

| 列名 | 说明 |
|------|------|
| 勾选 | 行勾选；表头支持全选当前页；左侧可展开/收起 |
| 产品 ID | 产品主数据 ID |
| 商品图片 | 产品主图缩略图 |
| 产品名称 | 产品名称 |
| 站点 | 限价适用站点 |
| 刊登最低金额范围 | 子级 ERPSKU 刊登最低金额的最小值 ~ 最大值（含币种） |
| 审核状态 | 待审核 / 审核通过 / 拒绝 |
| 添加人 | 录入操作人 |
| 审核人 | 审核操作人；待审核时为空 |
| 添加时间 | 录入时间，格式 `YYYY-MM-DD HH:mm:ss` |
| 备注 | 补充说明；可为空 |
| 操作 | 审核 / 查看、日志 |

**展开明细**  
主行默认不展示 ERPSKU；仅通过列表最左侧的展开箭头展开/收起，下方展示子表：**ERPSKU**、**刊登最低金额**。

**操作列**

- **审核**：仅「待审核」显示；打开审核弹窗，可修改并提交。
- **查看**：「审核通过」「拒绝」显示；按钮置灰；打开只读弹窗，不可修改。
- **日志**：打开操作日志弹窗。

**删除**  
勾选一行或多行后点击「删除」；确认后删除选中记录。未勾选时提示先勾选。

#### 1.3 分页

服务端分页；底栏展示总条数与页码（与参考原型 footer-tip 一致）。

---

### 录入限价数据

点击「录入限价数据」打开弹窗。

| 字段 | 控件 | 规则 |
|------|------|------|
| 平台 | 下拉框 | 必选；默认 `amazon`；选项：shopee、lazada、shopify、headline、amazon、shopline、aliexpress、coupang、11st、tikshop、hbs、gmk、mercado、allegro、ozon、qoo10、naver、interpark、wemakeprice、rakuten、ebay、yahoo、douyin、kuaishou、daraz、tokopedia、temu、noon、cdiscount、wildberries、Walmart、shopbase、etsy、emag、shein、red、Thaimart、alibaba、common |
| 站点 | 下拉框 | 必选；**默认日本**；币种随站点变化 |
| 产品 ID | 输入框 | 必填；输入后**自动带出**该产品下全部 ERPSKU |
| ERPSKU + 刊登最低金额 | 多行列表 | 每行一个 ERPSKU（下拉）与刊登最低金额；必填；金额 &gt; 0 |

**同步**  
第一行「同步」按钮：将第一行刊登最低金额快速填充到全部行。

**行操作**  
每行支持「+」新增、「×」删除；至少保留一行。

**取消 / 确定**  
确定后生成**一条主数据**（含多个 ERPSKU 子项），状态为「待审核」；主列表不展示 ERPSKU，需展开查看。

---

### 审核

点击行内「审核」打开审核弹窗。

**布局**：与「录入限价数据」弹窗相同，**顶部增加「审核」下拉框**。

| 审核下拉选项 | 说明 |
|-------------|------|
| 待审核 | 保持或改回待审核（管理人员可选） |
| 审核通过 | 通过并生效 |
| 拒绝 | 拒绝，需填写备注（建议必填，可在实现期确认） |

**字段**（与录入一致，可编辑或只读由权限决定；原型为可编辑）：

- 站点  
- 产品 ID  
- 刊登最低金额  

**取消 / 确定**  
确定后更新审核状态、审核人、审核时间，并写入操作日志。

---

### 操作日志

弹窗展示该条记录的操作历史，列：**操作人**、**操作内容**、**操作时间**。

典型日志内容示例：

- 录入限价：站点=日本，产品 ID=xxx，刊登最低金额=xxx  
- 审核通过 / 审核拒绝  
- 修改限价（若后续支持编辑）

---

### 限价数据（Tab）

模块顶部增加 Tab 切换：**限价管理** | **限价数据**。本 Tab 仅展示**当前售价低于对应刊登最低金额**的 Listing 数据；未匹配到刊登最低金额或售价不低于刊登最低金额的数据不进入列表。

#### 筛选

| 字段 | 控件 | 规则 |
|------|------|------|
| 产品 ID | 文本输入框 | 默认为空 |
| 产品信息 | 文本输入框 | 对 Listing 标题、产品列表标题模糊匹配；支持多条，以英文逗号分隔 |
| 卖家 SKU | 文本输入框 | 默认为空 |
| ERPSKU | 文本输入框 | 模糊匹配；支持多条，以英文逗号分隔 |
| ASIN | 文本输入框 | 对父 ASIN、子 ASIN 模糊匹配；支持多条，以英文逗号分隔 |
| 站点 | 下拉框 | 默认为空（全部） |
| 店铺 | 下拉框 | 默认为空（全部） |
| 链接负责人 | 下拉框 | 默认为空（全部） |
| 出现次数 | 数字输入框 | 精确匹配；默认为空 |
| 处理状态 | 下拉框 | 待处理、处理中、修改售价成功、修改售价失败、删除链接成功、删除链接失败；默认为空（全部） |

支持「查询」「重置」。

**批量修改售价**  
勾选一条或多条 Listing 后点击「批量修改售价」，弹窗展示所选商品及当前售价、新售价；支持“统一利润率 / 统一售价 / 售价加 / 售价减 / 售价乘”。仅「统一利润率」的输入值后展示 `%`，其余模式不展示单位；确认后回写列表售价。

仅「待处理」状态允许修改售价。批量选择中存在“处理中、修改售价成功、修改售价失败、删除链接成功、删除链接失败”时，提示“存在不是待处理状态链接”并阻止操作；单条操作时提示当前链接不是待处理状态。

**批量删除链接**  
勾选一条或多条 Listing 后点击「批量删除链接」，删除对应数据在其它位置的 Listing 链接。该操作**不删除限价数据信息**；操作后当前记录保留，并显示链接已删除。

仅「待处理」状态允许删除链接；单条及批量操作的状态校验规则与修改售价一致。

#### 列表展示

| 列名 | 说明 |
|------|------|
| 图片 | 产品图片缩略图 |
| 产品信息 | 同时展示 Listing 标题和产品列表标题 |
| 产品 ID | Listing 关联产品 ID |
| 卖家 SKU | Listing 卖家 SKU |
| ASIN | 每行展示父 ASIN、子 ASIN 两条数据 |
| ERPSKU | Listing 关联 ERPSKU |
| 站点 | 站点 |
| 店铺 | 店铺 |
| 链接负责人 | 链接负责人 |
| 售价 | 当前售价，展示站点币种 |
| 刊登最低金额 | 匹配产品 ID + ERPSKU 对应的刊登最低金额，展示站点币种；未匹配时显示 `--` |
| 处理状态 | 待处理 / 处理中 / 修改售价成功 / 修改售价失败 / 删除链接成功 / 删除链接失败 |
| 出现次数 | 命中 / 出现次数 |
| 查询时间 | 查询时间，格式 `YYYY-MM-DD HH:mm:ss` |
| 操作 | 待处理/处理中显示修改售价、删除链接、日志；修改售价成功/失败、删除链接成功/失败显示查看、日志 |

**查看**  
当处理状态为“修改售价成功、修改售价失败、删除链接成功、删除链接失败”时，操作列变为「查看」「日志」。点击「查看」复用售价信息弹窗，所有输入框和操作控件置灰，不允许修改。

**日志**  
点击「日志」打开弹窗，展示：修改人、修改内容、修改时间。修改售价和删除外部链接后自动追加日志。

---

### 非功能与其它

**性能**  
列表分页加载；图片缩略图懒加载（上线实现）。

**权限**  
查看、录入、审核按角色控制；与系统统一 RBAC 对齐。

**日志与审计**  
记录添加人、审核人、状态变更、金额变更及时间戳。

**兼容**  
Chrome / Edge 最新两个主版本。

---

### 验收要点（摘）

1. 仅具备文档查看权限的角色可进入模块；所有人录入均为待审核，由管理人员审核。  
2. 筛选：产品 ID / 添加人 / 审核人支持多空格或逗号分隔精确匹配；站点、审核状态支持多选；重置清空全部条件。  
3. 列表字段完整展示；待审核 / 拒绝行显示可用「审核」按钮。  
4. 录入弹窗：站点默认日本；刊登最低金额旁展示对应币种（如 JPY）；必填校验生效。  
5. 审核弹窗：顶部有审核下拉（待审核 / 审核通过 / 拒绝）；币种随站点联动；提交后状态与审核人正确回写。  
6. 日志弹窗可查看该条记录操作历史。

---

*本文档结构对齐《成本价变动通知》需求文档（需求名称 / 需求背景 / 需求方案 / 文档编辑人 / 需求详情——分「筛选」「列表」「录入」「审核」「日志」及子模块说明）。*
