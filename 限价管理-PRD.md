# 亚马逊限价管理 PRD

## 需求名称

亚马逊限价管理

## 需求背景

亚马逊运营需对指定平台、站点及产品下的多个 ERPSKU 维护刊登最低金额，以控制 Listing 售价底线。系统需统一完成限价录入、审核、低价 Listing 识别、售价修改、外部链接删除及操作审计，避免手工查询和漏处理。

## 需求方案

1. 新增「亚马逊限价管理」独立模块：具备文档查看权限的角色可访问；左侧导航展示名称：**亚马逊限价管理**。
2. **统一审核**：所有人录入的数据均进入「待审核」，由管理人员另行审核（通过 / 拒绝）。
3. 「限价管理」Tab 支持按产品维度维护多条 ERPSKU 限价，并通过主从展开结构查看明细。
4. 「限价数据」Tab 仅展示售价低于刊登最低金额的 Listing，支持修改售价、批量修改售价、删除外部链接、批量删除外部链接及日志。
5. 刊登最低金额和售价按站点展示币种（如日本站显示 JPY）。
6. 所有关键操作均记录日志，处理结果通过处理状态追踪。

**原型文件**：`亚马逊限价管理.html`

---

## 文档编辑人

待定

---

## 需求详情

**页面放置**  
亚马逊相关菜单下（与现有导航结构对齐；左侧导航展示名称：**亚马逊限价管理**。）

**现有说明**  
原型顶栏名称为「亚马逊限价管理原型」，上线时替换为统一系统顶栏。页面包含 **限价管理**、**限价数据**两个 Tab。

**原型链接（当前静态页）**  
本地文件：`file:///C:/Users/Admin/11110/亚马逊限价管理/亚马逊限价管理.html`

公开访问地址：`https://li6286621-cell.github.io/amazon-price-limit-live/`

---

### 权限与数据录入

#### 0.1 模块访问

| 角色类型 | 访问权限 | 录入 | 审核 |
|---------|---------|------|------|
| 仅文档查看权限 | 可访问列表与详情 | 否 | 否 |
| 运营人员 | 可访问 | 可录入，提交后待审核 | 否 |
| 管理人员 | 可访问 | 可录入，提交后同样待审核 | 可审核 |

**说明**：「仅拥有文档查看权限的角色可访问」指该模块对具备文档查看权限的用户开放；具体增删改、审核能力由角色细分控制（与系统统一 RBAC 对齐）。

#### 0.2 录入与审核规则

| 操作 | 审核状态 | 说明 |
|------|---------|------|
| 任何人录入 | 待审核 | 审核人为空，需管理人员审核 |
| 管理人员审核通过 | 审核通过 | 写入审核人 |
| 管理人员拒绝 | 拒绝 | 写入审核人；建议填写备注 |

**拒绝后**：可再次打开「审核」调整数据并改状态（本期列表「审核」按钮对待审核、拒绝状态可见）。

---

### 限价管理列表页

#### 1.1 筛选查询功能

**产品 ID**  
文本输入框；**精确匹配**；支持多个，以**空格或英文逗号**分隔；默认为空。

**平台**  
下拉选择框；选项与录入、审核弹窗的平台选项一致；默认为空（表示全部）。

**站点**  
下拉选择框；选项为业务站点（日本、美国、英国、德国、法国、加拿大、澳大利亚）；默认为空（表示全部）。

**审核状态**  
下拉选择框；枚举：**待审核**、**审核通过**、**拒绝**；默认为空（表示全部）。

**添加人**  
下拉选择框；选项为系统用户；默认为空（全部）。

**审核人**  
下拉选择框；选项为系统用户；默认为空（全部）。

**查询**  
点击查询按钮发起列表查询；从第一页加载（已接服务端分页时）。

**重置**  
清空所有筛选项；下拉恢复为空（全部）。

**单项清除**  
限价管理和限价数据两个 Tab 的筛选控件为空时保持默认样式；选择或输入内容后，右侧按钮切换为 `×`，点击后仅清空当前筛选项并恢复默认按钮。

**布局**  
「查询」「重置」按钮紧靠筛选项后；其下方单独一行放置「录入限价数据」「删除」按钮。

#### 1.2 列表展示

| 列名 | 说明 |
|------|------|
| 勾选 | 行勾选；表头支持全选当前页 |
| 展开 | 独立展开列；点击箭头展开/收起 ERPSKU 明细 |
| 图片 | 产品主图缩略图 |
| 产品信息 | 产品名称等基本信息 |
| 产品 ID | 产品主数据 ID，位于产品信息右侧 |
| 平台 | 位于站点左侧；当前原型数据均展示 `amazon` |
| 站点 | 限价适用站点 |
| 刊登最低金额范围 | 子级 ERPSKU 刊登最低金额的最小值 ~ 最大值（含币种） |
| 审核状态 | 待审核 / 审核通过 / 拒绝 |
| 添加人 | 录入操作人 |
| 审核人 | 审核操作人；待审核时为空 |
| 添加时间 | 录入时间，格式 `YYYY-MM-DD HH:mm:ss` |
| 备注 | 补充说明；可为空 |
| 操作 | 审核 / 查看、日志 |

**展开明细**  
主行默认不展示 ERPSKU；仅通过列表最左侧的展开箭头展开/收起，下方展示子表：**ERPSKU**、**刊登最低金额**。

**操作列**

- **审核**：仅「待审核」显示；打开审核弹窗，可修改并提交。
- **查看**：「审核通过」「拒绝」显示；按钮置灰；打开只读弹窗，所有内容不可修改。
- **日志**：打开操作日志弹窗。

**删除**  
勾选一行或多行后点击「删除」；确认后删除选中记录。未勾选时提示先勾选。

#### 1.3 分页

服务端分页；底栏展示总条数与页码（与参考原型 footer-tip 一致）。

---

### 录入限价数据

点击「录入限价数据」打开弹窗。

| 字段 | 控件 | 规则 |
|------|------|------|
| 平台 | 下拉框 | 必选；默认 `amazon`；选项：shopee、lazada、shopify、headline、amazon、shopline、aliexpress、coupang、11st、tikshop、hbs、gmk、mercado、allegro、ozon、qoo10、naver、interpark、wemakeprice、rakuten、ebay、yahoo、douyin、kuaishou、daraz、tokopedia、temu、noon、cdiscount、wildberries、Walmart、shopbase、etsy、emag、shein、red、Thaimart、alibaba、common |
| 站点 | 下拉框 | 必选；**默认日本**；币种随站点变化 |
| 产品 ID | 输入框 | 必填；输入后**自动带出**该产品下全部 ERPSKU；原型模拟带出 10 条 |
| ERPSKU + 刊登最低金额 | 多行列表 | 弹窗打开默认显示一行；ERPSKU 自动带出后仍允许修改；刊登最低金额必填且 &gt; 0；超过 10 条后列表固定高度并支持上下滚动 |
| 备注 | 多行文本框 | 非必填；提交后与审核弹窗共用同一备注字段 |

**同步**  
第一行「同步」按钮：将第一行刊登最低金额快速填充到全部行。

**行操作**  
每行支持「+」新增、「×」删除；至少保留一行。点击「+」后新增空白行，ERPSKU 由用户自行填写，不自动生成。

**取消 / 确定**  
确定后生成**一条主数据**（含多个 ERPSKU 子项），状态为「待审核」；主列表不展示 ERPSKU，需展开查看。

---

### 审核

点击行内「审核」打开审核弹窗。

**布局**：与「录入限价数据」弹窗相同，**顶部增加「审核」下拉框**。

| 审核下拉选项 | 说明 |
|-------------|------|
| 待审核 | 保持或改回待审核（管理人员可选） |
| 审核通过 | 通过并生效 |
| 拒绝 | 拒绝，需填写备注（建议必填，可在实现期确认） |

**字段**：

- 平台
- 站点  
- 产品 ID  
- ERPSKU（允许修改；查看状态下只读）
- 刊登最低金额
- 备注（回显录入备注并允许修改；拒绝时必填）

**取消 / 确定**  
确定后更新审核状态、审核人、审核时间，并写入操作日志。

---

### 操作日志

弹窗展示该条记录的操作历史，列：**操作人**、**操作内容**、**操作时间**。

典型日志内容示例：

- 录入限价：站点=日本，产品 ID=xxx，刊登最低金额=xxx  
- 审核通过 / 审核拒绝  
- 修改限价（若后续支持编辑）

---

### 限价数据（Tab）

模块顶部增加 Tab 切换：**限价管理** | **限价数据**。本 Tab 仅展示**当前售价低于对应刊登最低金额**的 Listing 数据；未匹配到刊登最低金额或售价不低于刊登最低金额的数据不进入列表。

#### 筛选

| 字段 | 控件 | 规则 |
|------|------|------|
| 产品 ID | 文本输入框 | 默认为空 |
| 产品信息 | 文本输入框 | 对 Listing 标题、产品列表标题模糊匹配；支持多条，以英文逗号分隔 |
| 卖家 SKU | 文本输入框 | 默认为空 |
| ERPSKU | 文本输入框 | 模糊匹配；支持多条，以英文逗号分隔 |
| ASIN | 文本输入框 | 对父 ASIN、子 ASIN 模糊匹配；支持多条，以英文逗号分隔 |
| 站点 | 下拉框 | 默认为空（全部） |
| 店铺 | 下拉框 | 默认为空（全部） |
| 链接负责人 | 下拉框 | 默认为空（全部） |
| 出现次数 | 数字输入框 | 精确匹配；默认为空 |
| 处理状态 | 下拉框 | 待处理、处理中、修改售价成功、修改售价失败、删除链接成功、删除链接失败；默认为空（全部） |

支持「查询」「重置」。

**批量修改售价**  
勾选一条或多条 Listing 后点击「批量修改售价」，弹窗展示所选商品及当前售价、新售价；支持“统一利润率 / 统一售价 / 售价加 / 售价减 / 售价乘”。仅「统一利润率」的输入值后展示 `%`，其余模式不展示单位；确认后回写列表售价。

计算规则：

- 统一利润率：`新售价 = 当前售价 × (1 + 输入值 / 100)`
- 统一售价：所有选中项的新售价等于输入值
- 售价加：`新售价 = 当前售价 + 输入值`
- 售价减：`新售价 = 当前售价 - 输入值`，结果不得小于 0
- 售价乘：`新售价 = 当前售价 × 输入值`

仅「待处理」状态允许修改售价。批量选择中存在“处理中、修改售价成功、修改售价失败、删除链接成功、删除链接失败”时，提示“存在不是待处理状态链接”并阻止操作；单条操作时提示当前链接不是待处理状态。

**批量删除listing链接**  
勾选一条或多条 Listing 后点击「批量删除listing链接」，删除对应数据在其它位置的 Listing 链接。该操作**不删除限价数据信息**；操作后当前记录保留，并显示链接已删除。

仅「待处理」状态允许删除listing链接；单条及批量操作的状态校验规则与修改售价一致。

#### 列表展示

| 列名 | 说明 |
|------|------|
| 图片 | 产品图片缩略图 |
| 产品信息 | 同时展示 Listing 标题和产品列表标题 |
| 产品 ID | Listing 关联产品 ID |
| 卖家 SKU | Listing 卖家 SKU |
| ERPSKU | Listing 关联 ERPSKU |
| ASIN | 每行展示父 ASIN、子 ASIN 两条数据 |
| 平台 | 位于站点左侧；当前原型数据均展示 `amazon` |
| 站点 | 站点 |
| 店铺 | 店铺 |
| 链接负责人 | 链接负责人 |
| 售价 | 当前售价，展示站点币种 |
| 刊登最低金额 | 匹配产品 ID + ERPSKU 对应的刊登最低金额，展示站点币种；未匹配时显示 `--` |
| 处理状态 | 待处理 / 处理中 / 修改售价成功 / 修改售价失败 / 删除链接成功 / 删除链接失败 |
| 出现次数 | 命中 / 出现次数 |
| 创建时间 | 创建时间，格式 `YYYY-MM-DD HH:mm:ss` |
| 操作 | 待处理/处理中显示修改售价、删除listing链接、日志；修改售价成功/失败、删除链接成功/失败显示查看、日志 |

**查看**  
当处理状态为“修改售价成功、修改售价失败、删除链接成功、删除链接失败”时，操作列变为「查看」「日志」。点击「查看」复用售价信息弹窗，所有输入框和操作控件置灰，不允许修改。

**日志**  
点击「日志」打开弹窗，展示：修改人、修改内容、修改时间。修改售价和删除外部链接后自动追加日志。

#### 处理状态流转

- 新进入限价数据：待处理。
- 发起异步处理后：处理中。
- 修改售价成功 / 失败：分别进入“修改售价成功”“修改售价失败”。
- 删除外部链接成功 / 失败：分别进入“删除链接成功”“删除链接失败”。
- 修改售价后若售价已不低于刊登最低金额，该条 Listing 不再满足进入条件，从限价数据列表移除。
- 删除链接只删除其它位置的 Listing 链接，不删除本模块限价数据。

---

### 非功能与其它

**性能**  
列表分页加载；图片缩略图懒加载（上线实现）。

**权限**  
查看、录入、审核按角色控制；与系统统一 RBAC 对齐。

**日志与审计**  
记录添加人、审核人、状态变更、金额变更及时间戳。

**兼容**  
Chrome / Edge 最新两个主版本。

---

### 验收要点（摘）

1. 具备文档查看权限的角色可进入「亚马逊限价管理」；页面包含“限价管理”“限价数据”两个 Tab。  
2. 所有人录入后状态均为待审核，由管理人员审核；审核通过和拒绝后仅允许查看。  
3. 限价管理支持按产品 ID、平台、站点、审核状态、添加人和审核人筛选；查询、重置正确，所有下拉选项完整。
4. 录入弹窗平台默认 amazon、站点默认日本；输入产品 ID 后带出 ERPSKU；同步可将首行刊登最低金额填充到全部行。  
5. 主列表不直接展示 ERPSKU；点击最左侧独立展开箭头后，居中展示 ERPSKU、刊登最低金额子表。  
6. 限价数据仅展示售价低于刊登最低金额的 Listing；图片、产品信息、产品 ID、卖家 SKU、ERPSKU、父/子 ASIN、售价、限价及处理状态展示正确。  
7. 产品信息、ERPSKU、ASIN 支持英文逗号分隔的多值模糊查询；处理状态筛选正确。  
8. 仅待处理数据可修改售价或删除listing链接；单条和批量选择存在非待处理数据时，能提示并阻止操作。
9. 修改售价支持统一利润率、统一售价、售价加、售价减、售价乘；仅统一利润率显示 `%`。  
10. 删除链接不会删除限价数据；终态操作列显示“查看、日志”，查看弹窗内容置灰且不可修改。  
11. 限价管理日志和限价数据日志均可追溯对应操作人、内容及时间。

---

*本文档对应当前《亚马逊限价管理》HTML 原型，包含限价维护、审核、低价 Listing 处理及日志审计。*
